import { StatusValidatorPipe } from './status-validator.pipe';

describe('StatusValidatorPipe', () => {
  it('should be defined', () => {
    expect(new StatusValidatorPipe()).toBeDefined();
  });
});
